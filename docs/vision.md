# Xero Vault — ビジョン

> 本ドキュメントはコード・既存機能から逆算した現状のドラフトです。プロダクトの意図と食い違う点があれば書き換えてください。
>
> **軸(2026-07-20確定、`docs/rebuild.md`参照)**: Xerovaultの核は「タグ類似度による仲間探し・マッチング」ではなく、以下に書く「既存グループ内のゴール管理・アカウンタビリティ」。タグは現状シンプルな完全一致フィルタのままで良く、類似度マッチングは将来フェーズへ後回しにする。

## コンセプト

個人の目標管理アプリの多くは「立てて終わり」になりがちで、達成の可否を自己申告に委ねるため曖昧な目標(「頑張る」「継続する」)が量産される。Xero Vaultは、目標をグループ内の**相互投票による合意形成**で達成扱いにすることで、次の2つを強制する。

1. **曖昧な目標を許さない** — 期限・担当者を明示した「具体的な目標」は、そうでない目標より高く評価される(`docs/score-design.md`参照)。
2. **自己申告を許さない** — 達成の可否はグループメンバーの投票(YES/NO)によって決まり、本人だけでは達成を宣言できない。

## 誰のためのアプリか

少人数(サークル・プロジェクトチーム・自習グループなど)で、互いの進捗に責任を持たせ合いたいグループ。

## コア体験

1. グループ(Group)を作り、メンバーを招待する
2. グループ内でゴール(Goal)を立てる。期限と担当者を設定するほど「具体的な目標」として扱われる
3. メンバーがゴールの達成についてYES/NO投票する。投票率が閾値(90%)を超えると達成扱いになる
4. 達成したゴールはグループのスコアに加算され、連続達成でストリークボーナスが付く
5. 公開グループはランキングで他グループと比較できる

## 期限切れ・未達成の扱い

期限(`deadline`)と担当者(`assigneeId`)を両方持つ「具体的な目標」は、期限を過ぎても未達成のままだと`missed`状態になりスコアが減点される。曖昧な目標にはこのリスクがない代わりに得点も低い。詳細は `docs/score-design.md` を参照。

## リアルタイム更新の現状

投票・チャットの即時反映はSupabase Realtime(`postgres_changes`購読)で部分的に実装済み(ゴール詳細ページ `GoalDetail.vue` のみ)。ただし以下は未対応で、ページ再読み込みが必要:

- グループのスコア/streakの変更は他クライアントにpushされない
- 投票していないメンバーの画面では、ゴールのステータスバッジや投票ボタンの表示が自動更新されない
- ゴール詳細ページ以外(グループダッシュボード・ランキングなど)は購読していない

## 現状のスコープ外(将来検討)

- 本番デプロイパイプライン
